# Syncr Storybook (React)

Interactive docs to try Syncr features (URL + storage + broadcast + Zod).

## Run
```bash
pnpm install
pnpm --filter syncr-storybook-react storybook
# open http://localhost:6006
```
