# Syncr React Demo

## Run
```bash
pnpm install
pnpm --filter syncr-react-demo dev
```

(From repo root, you can also `cd examples/react-app && pnpm dev`.)

The demo uses local workspace links to `@syncr/core` and `@syncr/react`.
